# Throwaway EOA that pays deployment gas (fund via a Base Sepolia faucet,
# e.g. Coinbase Developer Platform or Alchemy faucet).
DEPLOYER_KEY=0x...

# Your Privy SMART WALLET address (shown in the app header after login) —
# NOT the deployer key's address, NOT the embedded EOA.
FIRST_MEMBER=0x...
FIRST_MEMBER_NAME=Timothy

# For forge --verify on Basescan (free key from basescan.org)
BASESCAN_API_KEY=

# Base Sepolia RPC (public endpoint works; an Alchemy key is more reliable)
RPC_URL=https://sepolia.base.org
